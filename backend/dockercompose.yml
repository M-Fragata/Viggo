services:
  
  postgres:
    image: "bitnami/postgresql:latest"
    ports:
      - "5433:5432"
    environment:
      POSTGRES_NAME: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: viggo